# L0 - Creating and Removing Elements

This project is a simple demonstration of DOM manipulation using JavaScript.  
It includes adding and removing paragraph elements dynamically using:

- `createElement()`
- `appendChild()`
- `removeChild()`

---

## 🚀 Features

- **Add Paragraph** button: Creates a new paragraph with the text  
  *"This is a new paragraph."*
- **Remove Last Paragraph** button: Deletes the most recently added paragraph.
- Uses core DOM methods for element creation and removal.

---

## 🗂 Project Structure

